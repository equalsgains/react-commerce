export const initialState = {
    basket: [],
    
}

function reducer(state, action) {
    switch(action.type) {
        case 'ADD_TO_BASKET':
            // LOGIG FOR ADDING ITEM TO BASKET
            break;
        case 'REMOVE_FROM_BASKET':
            break;
    }
}